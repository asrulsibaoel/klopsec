apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seldon.fullname" . }}-test-connection"
  labels:
    {{- include "seldon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seldon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
