apiVersion: v1
kind: Service
metadata:
  name: {{ include "seldon.fullname" . }}
  labels:
    {{- include "seldon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seldon.selectorLabels" . | nindent 4 }}
